import { useEffect, useRef } from 'react';
import { Link } from 'react-router-dom';
import { motion, useInView } from 'framer-motion';
import {
  Zap, Brain, Code2, BarChart3, Shield, Star,
  ArrowRight, CheckCircle, Users, Award, TrendingUp, Sparkles,
  FileText, Target, Cpu
} from 'lucide-react';
import { Button } from '@/components/ui/index.jsx';
import Navbar from '@/components/common/Navbar';

// ─── Animation variants ───────────────────────────────────────
const fadeUp = {
  hidden: { opacity: 0, y: 30 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: 'easeOut' } },
};

const stagger = {
  visible: { transition: { staggerChildren: 0.1 } },
};

// ─── Data ─────────────────────────────────────────────────────
const features = [
  {
    icon: FileText, color: 'text-brand-400', bg: 'bg-brand-500/15',
    title: 'AI Resume Parser',
    desc: 'Upload your PDF/DOCX and our AI extracts structured data — skills, experience, education — in seconds.',
  },
  {
    icon: Target, color: 'text-accent-400', bg: 'bg-accent-500/15',
    title: 'ATS Score Analysis',
    desc: 'Get a brutal, honest ATS score with breakdown, missing keywords, and specific improvement suggestions.',
  },
  {
    icon: Brain, color: 'text-cyan-400', bg: 'bg-cyan-500/15',
    title: 'AI Test Generation',
    desc: 'Skill-tailored MCQ and coding assessments generated by LLMs based on your resume profile.',
  },
  {
    icon: Code2, color: 'text-emerald-400', bg: 'bg-emerald-500/15',
    title: 'Live Code Execution',
    desc: 'Monaco Editor with multi-language support, hidden test cases, and AI code quality review.',
  },
  {
    icon: Cpu, color: 'text-purple-400', bg: 'bg-purple-500/15',
    title: 'Semantic Matching',
    desc: 'Vector embeddings match your profile to internships with cosine similarity + AI re-ranking.',
  },
  {
    icon: BarChart3, color: 'text-orange-400', bg: 'bg-orange-500/15',
    title: 'Recruiter Analytics',
    desc: 'Placement funnel, skill demand charts, and ATS distribution analytics for hiring teams.',
  },
];

const stats = [
  { value: '10K+',  label: 'Students Matched',  icon: Users },
  { value: '94%',   label: 'Match Accuracy',    icon: Target },
  { value: '500+',  label: 'Partner Companies', icon: Award },
  { value: '3.2×',  label: 'Faster Hiring',     icon: TrendingUp },
];

const steps = [
  { step: '01', title: 'Upload Resume', desc: 'Drop your PDF or DOCX. Our AI parses and scores it instantly.', icon: FileText },
  { step: '02', title: 'Take Assessment', desc: 'Complete AI-generated MCQs and coding challenges tailored to your skills.', icon: Brain },
  { step: '03', title: 'Get Matched', desc: 'Receive ranked internship recommendations with detailed match explanations.', icon: Sparkles },
];

// ─── Landing Page ─────────────────────────────────────────────
const Landing = () => {
  return (
    <div className="min-h-screen bg-background text-foreground overflow-hidden">
      <Navbar />

      {/* ─── Hero Section ─────────────────────────────────── */}
      <section className="relative min-h-screen flex items-center justify-center pt-16">
        {/* Animated background */}
        <div className="absolute inset-0 bg-animated-gradient" />

        {/* Decorative orbs */}
        <div className="orb orb-1 top-20 left-[10%]" />
        <div className="orb orb-2 top-40 right-[15%]" />
        <div className="orb orb-3 bottom-32 left-[30%]" />

        {/* Grid overlay */}
        <div
          className="absolute inset-0 opacity-[0.03]"
          style={{
            backgroundImage: 'linear-gradient(#16a34a 1px, transparent 1px), linear-gradient(to right, #16a34a 1px, transparent 1px)',
            backgroundSize: '60px 60px',
          }}
        />

        <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <motion.div
            variants={stagger}
            initial="hidden"
            animate="visible"
            className="space-y-8"
          >
            {/* Badge */}
            <motion.div variants={fadeUp} className="flex justify-center">
              <span className="inline-flex items-center gap-2 px-4 py-2 bg-brand-500/15 border border-brand-500/30 rounded-full text-sm font-medium text-brand-400">
                <Sparkles className="w-3.5 h-3.5" />
                AI-Powered · LLM-Driven · Real-time
              </span>
            </motion.div>

            {/* Headline */}
            <motion.h1 variants={fadeUp} className="text-5xl sm:text-6xl lg:text-7xl font-black tracking-tight">
              Land your dream
              <br />
              <span className="gradient-text">internship with AI</span>
            </motion.h1>

            {/* Subheadline */}
            <motion.p variants={fadeUp} className="text-lg sm:text-xl text-muted max-w-2xl mx-auto leading-relaxed">
              InternIQ automates the entire internship pipeline — from intelligent resume parsing
              and ATS scoring to AI assessments, code evaluation, and semantic job matching.
            </motion.p>

            {/* CTA Buttons */}
            <motion.div variants={fadeUp} className="flex flex-col sm:flex-row gap-4 justify-center">
              <Link to="/register">
                <Button size="xl" id="hero-get-started-btn" className="group">
                  Get Started Free
                  <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
                </Button>
              </Link>
              <Link to="/login">
                <Button size="xl" variant="secondary" id="hero-signin-btn">
                  Sign In
                </Button>
              </Link>
            </motion.div>

            {/* Trust signals */}
            <motion.div variants={fadeUp} className="flex flex-wrap items-center justify-center gap-6 text-sm text-muted">
              {['No credit card required', 'Free to get started', 'GDPR compliant'].map((text) => (
                <div key={text} className="flex items-center gap-1.5">
                  <CheckCircle className="w-4 h-4 text-success-500" />
                  {text}
                </div>
              ))}
            </motion.div>
          </motion.div>
        </div>

        {/* Scroll indicator */}
        <motion.div
          className="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2"
          animate={{ y: [0, 8, 0] }}
          transition={{ duration: 2, repeat: Infinity }}
        >
          <div className="w-6 h-10 border-2 border-surface-border rounded-full flex items-start justify-center p-1">
            <motion.div
              className="w-1.5 h-3 bg-brand-400 rounded-full"
              animate={{ y: [0, 12, 0] }}
              transition={{ duration: 1.5, repeat: Infinity }}
            />
          </div>
        </motion.div>
      </section>

      {/* ─── Stats Section ────────────────────────────────── */}
      <StatsSection />

      {/* ─── How It Works ─────────────────────────────────── */}
      <HowItWorks />

      {/* ─── Features Grid ────────────────────────────────── */}
      <FeaturesSection />

      {/* ─── CTA Section ──────────────────────────────────── */}
      <CTASection />

      {/* ─── Footer ───────────────────────────────────────── */}
      <Footer />
    </div>
  );
};

const StatsSection = () => {
  const ref = useRef(null);
  const inView = useInView(ref, { once: true });

  return (
    <section ref={ref} className="py-20 border-y border-surface-border/30">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid grid-cols-2 lg:grid-cols-4 gap-8">
          {stats.map(({ value, label, icon: Icon }, i) => (
            <motion.div
              key={label}
              initial={{ opacity: 0, y: 20 }}
              animate={inView ? { opacity: 1, y: 0 } : {}}
              transition={{ delay: i * 0.1, duration: 0.5 }}
              className="text-center"
            >
              <div className="w-12 h-12 mx-auto rounded-2xl bg-brand-500/15 flex items-center justify-center mb-4">
                <Icon className="w-6 h-6 text-brand-400" />
              </div>
              <div className="text-4xl font-black gradient-text mb-1">{value}</div>
              <div className="text-sm text-muted">{label}</div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

const HowItWorks = () => {
  const ref = useRef(null);
  const inView = useInView(ref, { once: true, margin: '-100px' });

  return (
    <section ref={ref} className="py-24">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={inView ? { opacity: 1, y: 0 } : {}}
          className="text-center mb-16"
        >
          <h2 className="text-4xl font-black mb-4">How it works</h2>
          <p className="text-muted max-w-xl mx-auto">Three steps to your perfect internship match</p>
        </motion.div>

        <div className="grid md:grid-cols-3 gap-8 relative">
          {/* Connector line */}
          <div className="hidden md:block absolute top-16 left-1/3 right-1/3 h-px bg-gradient-to-r from-brand-500/20 via-brand-500/60 to-brand-500/20" />

          {steps.map(({ step, title, desc, icon: Icon }, i) => (
            <motion.div
              key={step}
              initial={{ opacity: 0, y: 30 }}
              animate={inView ? { opacity: 1, y: 0 } : {}}
              transition={{ delay: i * 0.15 }}
              className="glass-card p-8 text-center relative"
            >
              <div className="relative w-16 h-16 mx-auto mb-6">
                <div className="w-16 h-16 rounded-2xl bg-brand-500/15 flex items-center justify-center">
                  <Icon className="w-7 h-7 text-brand-400" />
                </div>
                <span className="absolute -top-2 -right-2 w-6 h-6 bg-brand-500 text-white text-xs font-black rounded-full flex items-center justify-center">
                  {i + 1}
                </span>
              </div>
              <h3 className="text-xl font-bold mb-3">{title}</h3>
              <p className="text-muted text-sm leading-relaxed">{desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

const FeaturesSection = () => {
  const ref = useRef(null);
  const inView = useInView(ref, { once: true, margin: '-100px' });

  return (
    <section ref={ref} className="py-24 bg-subtle/50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={inView ? { opacity: 1, y: 0 } : {}}
          className="text-center mb-16"
        >
          <h2 className="text-4xl font-black mb-4">Everything you need</h2>
          <p className="text-muted max-w-xl mx-auto">
            A complete AI-driven hiring platform — from resume analysis to internship placement
          </p>
        </motion.div>

        <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {features.map(({ icon: Icon, color, bg, title, desc }, i) => (
            <motion.div
              key={title}
              initial={{ opacity: 0, y: 20 }}
              animate={inView ? { opacity: 1, y: 0 } : {}}
              transition={{ delay: i * 0.08 }}
              className="glass-card p-6 hover:border-brand-500/20 transition-all duration-300 group"
            >
              <div className={`w-12 h-12 rounded-2xl ${bg} flex items-center justify-center mb-5 group-hover:scale-110 transition-transform`}>
                <Icon className={`w-6 h-6 ${color}`} />
              </div>
              <h3 className="font-bold text-lg mb-2">{title}</h3>
              <p className="text-muted text-sm leading-relaxed">{desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

const CTASection = () => (
  <section className="py-24">
    <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <motion.div
        initial={{ opacity: 0, scale: 0.95 }}
        whileInView={{ opacity: 1, scale: 1 }}
        viewport={{ once: true }}
        className="glass-card p-12 relative overflow-hidden"
      >
        {/* Glow background */}
        <div className="absolute inset-0 bg-gradient-glow opacity-30" />
        <div className="relative z-10 space-y-6">
          <div className="w-16 h-16 mx-auto rounded-2xl bg-gradient-brand flex items-center justify-center shadow-glow-md">
            <Zap className="w-8 h-8 text-white fill-white" />
          </div>
          <h2 className="text-4xl font-black">Ready to land your internship?</h2>
          <p className="text-muted text-lg max-w-xl mx-auto">
            Join thousands of students who've accelerated their careers with InternIQ's AI-powered platform.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Link to="/register">
              <Button size="xl" id="cta-register-btn" className="group">
                Start for Free
                <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
              </Button>
            </Link>
          </div>
        </div>
      </motion.div>
    </div>
  </section>
);

const Footer = () => (
  <footer className="border-t border-surface-border/30 py-12">
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col sm:flex-row items-center justify-between gap-4">
      <div className="flex items-center gap-2">
        <div className="w-7 h-7 rounded-lg bg-gradient-brand flex items-center justify-center">
          <Zap className="w-3.5 h-3.5 text-white fill-white" />
        </div>
        <span className="font-black text-base">Intern<span className="gradient-text">IQ</span></span>
      </div>
      <p className="text-sm text-muted">© {new Date().getFullYear()} InternIQ. All rights reserved.</p>
      <div className="flex items-center gap-4 text-sm text-muted">
        <Link to="/privacy" className="hover:text-foreground transition-colors">Privacy</Link>
        <Link to="/terms"   className="hover:text-foreground transition-colors">Terms</Link>
      </div>
    </div>
  </footer>
);

export default Landing;
